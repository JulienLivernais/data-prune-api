services:

  app:
    build: .
    ports:
      - "8001:8000"
    env_file:
      - .env
    depends_on:
      - db
    networks:
      - dataprune_network

  db:
    image: postgres:15
    env_file:
      - .env
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - dataprune_network

volumes:
  postgres_data:

networks:
  dataprune_network: